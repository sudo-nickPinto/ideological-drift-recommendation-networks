# Collaboration Notes

- The user wants professor-style, step-by-step explanations with the "why" behind each line.
- Assume very basic Python knowledge unless proven otherwise.
- Correct misunderstandings directly and keep the process repeatable.
- When implementing a new module, match the explanatory format used in src/graph_builder.py and src/ideology.py.
- After implementing a module, create or update the matching pytest file in the same tutorial-heavy style.
- Run the relevant tests immediately after implementation and report the concrete result.
- Prefer deterministic tests for random behavior by using fixed seeds or forced graph structures.
- Do not skip basic explanations like dictionaries, loops, node attributes, edge attributes, or fixtures.